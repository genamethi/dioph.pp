[Unit]
Description=Primeparts native Iceberg REST Catalog daemon (primeparts-catalogd)
After=network.target

[Service]
Type=simple
ExecStart=@PP_INSTALL_PATH@/primeparts-catalogd --config @PP_INSTALL_PATH@/config.lua
Restart=on-failure
RestartSec=2

[Install]
WantedBy=default.target
