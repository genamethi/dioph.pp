# 05 — permanent-fixture decision

Deliberately last: what pp-graph the permanent tool is, decided from the
results of 02–04 rather than ahead of them.

- [ ] Tool shape: subcommands, what stays in the experiment and what
      graduates; whether the client module absorbs the planning-dispatch
      seam for the other binaries.
- [ ] Persistence: whether pp-graph produces tables at all (canonical-word
      census, per-prime graph attributes, nothing) — justified by the access
      patterns 02–04 actually exhibited, created through `init` at birth with
      declared sort order and spec'd write properties.
- [ ] The injectivity lemma (collapse classes = canonical words, Ritt-style
      argument with nonzero inter-power constants) written down properly —
      it underwrites the census representation if persistence is chosen.
- [ ] Merge call: what moves from `pp-graph-exp` toward `tui-query` — the
      user decides.
