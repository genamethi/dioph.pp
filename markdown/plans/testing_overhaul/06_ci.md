# 06 CI

deps: 05 | status: todo

Self-hosted promix GitHub Actions. No dep rebuild, no 1e9 generate, no sanitizer job.

- [ ] `.github/workflows/native.yml`: `runs-on: [self-hosted, promix]`; assumes configured prefix.
      Jobs: build + `make -C native test`; build + `make -C native e2e` (fixture path).
- [ ] boundary: workflow file valid; user confirms the self-hosted runner is registered before relying on it.
