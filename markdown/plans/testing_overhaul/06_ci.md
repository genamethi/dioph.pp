# 06 CI

deps: 05 | status: done (workflow authored; runner registration is a user action)

Self-hosted promix GitHub Actions. No dep rebuild, no 1e9 generate, no sanitizer job.

- [x] `.github/workflows/native.yml`: `runs-on: [self-hosted, promix]`; assumes configured prefix.
      One `native` job: checkout, init only the lmdb submodule (arrow/iceberg come from the prefix),
      `make -C native -j test`, then `make -C native e2e`.
- [x] workflow YAML validates.
- [ ] user confirms the self-hosted runner is registered before relying on it.
